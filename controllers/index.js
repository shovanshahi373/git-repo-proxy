const RepositoryController = require("./repository");
const RawContentController = require("./rawcontent");

module.exports = {
  RepositoryController: RepositoryController,
  RawContentController: RawContentController,
};
